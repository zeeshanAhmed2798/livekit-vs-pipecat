services:
  voxkit-livekit:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - LIVEKIT_URL
      - LIVEKIT_API_KEY
      - LIVEKIT_API_SECRET
      - ULTRAVOX_API_KEY
      - OPENAI_API_KEY
      - LIVEKIT_AGENT_NAME
    restart: unless-stopped